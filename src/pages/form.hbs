<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <link rel="icon" href="https://glitch.com/favicon.ico" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <title>Alô SQLite: {{operacao}} Produto</title>
  <link rel="stylesheet" href="/style.css" />
</head>

<body>
  <div class="wrapper">
    <div class="content" role="main">
      {{>cabecalho}}
      <h1>{{operacao}}Produto</h1>
{{#if error}}
      <p class="error">
        {{error}}
      </p>
{{else}}
      <!-- O usuário pode limpar a lista de votos se ele primeiramente determinar a ADMIN_KEY presente no .env-->
      <form action="{{funcao}}" method="post">
        <h3>
          {{operacao}} produto no produto
        </h3>
        <input type="text" id="nome" name="nome" required>
        <button type="submit">{{operacao}}</button>
      </form>
{{/if}}
    </div>
  </div>
  {{>rodape}}
</body>

</html>